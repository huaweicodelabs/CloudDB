import { AGCStorageService } from "@agconnect/storage-types";
import AGCStorage = AGCStorageService.AGCStorage;
import { AGCInstance } from "@agconnect/instance-types";
export declare class HarmonyStorage implements AGCStorage {
    private static harmonyStoreMap;
    private agcCryptImpl;
    static getInstance(instance: AGCInstance, cryptImpl?: any): HarmonyStorage;
    get(key: string): Promise<string | null>;
    remove(key: string): Promise<void>;
    set(key: string, value: any): Promise<void>;
}
