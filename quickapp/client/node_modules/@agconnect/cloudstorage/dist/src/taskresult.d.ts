import { TaskState } from './upload/taskenums';
import { Metadata } from './metadata';
import { StorageReference } from './storagereference';
import { UploadTask } from './task';
export declare class UploadResult {
    readonly bytesTransferred: number;
    readonly totalByteCount: number;
    readonly state: TaskState;
    readonly metadata: Metadata | null;
    readonly task: UploadTask;
    readonly reference: StorageReference;
    constructor(bytesTransferred: number, totalByteCount: number, state: TaskState, metadata: Metadata | null, task: UploadTask, reference: StorageReference);
}
