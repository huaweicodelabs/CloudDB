/**
 * @fileOverview this class is used to get agcconnect configuration
 */
import '@agconnect/instance';
import { AGCInstance } from '@agconnect/instance-types';
export declare class Config {
    private static instance;
    private _agcInstance;
    private _storageHosts;
    private _defaultBucket;
    private _productId;
    private _clientId;
    private _appId;
    private _defaultRegion;
    private _addressThreshold;
    constructor(inss: AGCInstance);
    static initInstance(ins: AGCInstance): Config;
    static getInstance(): Config;
    getHostByRegion(region: string): AddressCount;
    clearHostCount(region: string, index: number): void;
    setHostCount(region: string, index: number): void;
    get defaultBucket(): string;
    get productId(): string;
    get clientId(): string;
    get appId(): string;
    get defaultRegion(): string;
    private generateUrl;
}
export interface AddressCount {
    url: string;
    index: number;
    count: number;
}
