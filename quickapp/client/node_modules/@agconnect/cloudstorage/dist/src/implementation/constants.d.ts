/**
 * The key in agconnect config json for the storage bucket.
 */
export declare const CONFIG_STORAGE_BUCKET_KEY = "default_storage";
/**
 * 2 minutes
 *
 * The timeout for all operations except upload.
 */
export declare const DEFAULT_MAX_REQUEST_TIMEOUT: number;
/**
 * 10 minutes
 *
 * The timeout for upload.
 */
export declare const DEFAULT_MAX_UPLOAD_TIMEOUT: number;
/**
 * 3 times
 *
 * Request Retry Times
 */
export declare const DEFAULT_MAX_RETRY_TIMES = 3;
/**
 * This is the value of Number.MIN_SAFE_INTEGER, which is not well supported
 * enough for us to use it directly.
 */
export declare const MIN_SAFE_INTEGER = -9007199254740991;
